<div class="wrapper">
  <div class="content">
    <h2><%= @task.done == true ? "Done!" : "To do" %> | <%= @task.name %></h2>
    <p><%= @task.description %></p>
    <p>Importance: <%= @task.importance %></p>
  </div>
</div>
<!-- ajouter des infos, importance etc, création... -->
